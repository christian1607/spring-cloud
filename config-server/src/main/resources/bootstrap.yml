server:
  port: 8888
spring:
  profiles:
    active: default
  cloud:
    config:
      server:

        # for file system config properties
        #native:
        #  search-locations: /home/toshiba/Documents/proyectos/spring/cloud/spmia-chapter2/config-repository-local/license-service/
        git:
          uri: https://github.com/christian1607/spring-cloud/
          search-paths: config-remote-git/license-service
          username: christian1607
          password: Altamirano1607

